package com.yinfires.icecore.compat.cozycafe;

/** Stable bridge for the open state already synchronized by CozyCafe's manager menu. */
public interface CozyCafeMenuStateAccess {
    boolean icecore$isCafeOpen();
}
