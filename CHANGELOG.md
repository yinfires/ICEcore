# ICEcore 更新日志

## 0.1.0 - 2026-09-04

- Minecraft 版本：1.20.1
- 模组加载器：Forge 47.4.10
- 许可证：Apache-2.0
- 发布类型：功能更新

### 新增内容

- 新增 SDM Shop Rework 多商店管理：管理员可使用 `/icecore sdmshop` 创建、命名、重命名、删除、打开和进入指定商店编辑界面；旧默认商店会迁移为 `default`，并移除 SDM 商店按键与背包侧边栏入口。
- 新增固定 NPC 系统：管理员可按数据包定义生成和移除玩家模型 NPC；NPC 会注视附近玩家，右键后通过 ChatBox 打开对话。具体 NPC 及其对话由数据包定义，不内置在模组中。
- 新增全服共享虚拟货币：余额随世界保存，管理员可查询、设置、增加或扣除，并统一开关所有玩家左上角的货币 HUD；余额变化带有数字过渡与增减提示，数据包可通过命令、命令存储和函数标签接入购买等流程。
- 新增 SDM Shop Rework 联动：商店买入、卖出和余额显示统一使用 ICEcore 的全服共享货币，并显示 ICEcore 钱币图标。
- CozyCafe 顾客付款现在直接进入 ICEcore 的全服共享余额，不再掉落实体货币；同时安装 Create: Numismatics Utils 时也由 ICEcore 余额统一接管。
- 同时安装 CozyCafe 与 JEI 时，现在可以将 JEI 物品列表中的食物拖到咖啡馆电脑的菜单输入格，直接写入菜单。
- 在菜单编辑界面悬浮 JEI 物品列表中的可售食物时，现在会显示价格、分类和容器提示，与背包内同一物品一致。
- 在 CozyCafe 电脑菜单中通过 JEI 拖入、直接点击物品或 Shift+左键/右键连续写入时，每次成功或失败都会播放对应音效；直接拿起物品点击输入格后不会因同一次鼠标操作重复触发失败反馈。
- 新增 CozyCafe 咖啡馆电脑识别范围修改功能：管理员可手持普通木棍对准电脑并执行 `/icecore cozycafe range set`，再用左键和右键选择最多包含 65536 个方块的范围；完成后的范围覆盖电脑原有识别范围，不再随店铺声望或等级变化。
- 新增 CozyCafe 1.9 与森罗物语：酒馆联动：酒馆黑板和全部夹心展板可绑定咖啡馆电脑并同步名称、营业状态；管理员还可为电脑绑定 ICEcore 区域作为顾客进出位置，绑定有效时无需原咖啡馆标志，顾客进店与离场均可打开木门和栅栏门。
- 新增 CozyCafe 1.9 咖啡馆清空保护：营业期间点击电脑一键清空不会产生反馈或改变状态，须正常关业后再清空；同时会自动恢复旧错误流程遗留的占用菜单，避免最后顾客离开时造成存档崩溃。
- 新增冒险模式建造系统：管理员可配置区域、方块列表、承托列表和方向限制，冒险模式玩家可在规则允许时放置和拆除受管方块。
- 新增冒险模式禁丢弃与玩家给予：冒险模式下禁止所有物品掉落，关闭界面时鼠标物品会安全保留；准星对准玩家时可使用 Q 单个给予、长按连续给予或 Ctrl+Q 尽量给予整组。
- 新增主世界时间系统：管理员可配置昼夜耗时倍率、右上角日期时间 HUD 和自然换日结算动画；自然流逝到次日 06:00 时可显示昨日净收入，不影响配方、熔炉等正常游戏更新，并保持原版昼夜循环规则与时间指令可用。
- 新增冒险模式时间跳过：可复用区域和方块列表配置触发地点与摄像机，多人需共同确认后播放时间快进、昨日净收入提示和视角恢复过场；单人游戏无需等待其他玩家确认。
- 新增 Paladin's Furniture 资源加载兼容，避免同时安装时客户端偶发启动失败。
- 新增任务与教程系统：按 J 打开任务界面、按 I 打开教程界面（可在按键设置中改键），界面左侧为可折叠的分类列表、右侧为详情，详情以更大更粗的标题与横线分隔正文；详情正文下方以进度条形式列出任务目标，与描述分开显示，任务与教程详情均可内嵌图片；界面采用半透明黑色简洁风格，打开时默认不选中条目，内容过长时可用仅在滚动或悬停时显示的极简滚动条滚动或拖动。任务与教程默认隐藏，需通过命令或对话解锁后才显示；已完成的任务不再显示，教程解锁后带“新”标记、点开即视为已读。任务同时只能标记一个，获取新任务会自动改为标记新任务，被标记任务显示在屏幕右上角带边框的追踪栏中，位于时间 HUD 之下并随其显隐自动上下移动；追踪栏在进入正常视角时弹出、离开时收回，任务完成时目标进度补满并短暂停留后再收回。有未读的新任务或教程时，右上角会提示按 J/I 查看。任务、教程与分类均由数据包 JSON 定义，支持前置任务链与以命令表达的完成奖励；管理员可用 `/icecore quest` 与 `/icecore tutorial` 获取、完成、标记、解锁与重置，对话也可通过 ChatBox 事件接取任务或解锁教程，与固定 NPC、货币和商店联动。
- 新增 JEI 配方隐藏与合成禁止：管理员可用 `/icecore recipe hide all|show all` 一键隐藏或显示全部配方，用 `/icecore recipe hide item <物品>|show item <物品>` 隐藏或解锁某物品的所有产出配方。被隐藏的配方在 JEI 中搜不到、看不到，收藏夹中的对应项也随之失效，且在工作台、熔炉等原版合成中被真正禁止合成；按物品隐藏时该物品本身也从 JEI 物品列表消失，解锁时恢复显示，无可解锁配方的物品仅切换其显示。被隐藏物品所需的工作台、熔炉等催化剂物品也一并从 JEI 物品列表消失，但仍可正常点击查看未隐藏物品的配方。一键隐藏后仍可对单个物品单独解锁，其配方随即恢复可见与可合成，且可在 JEI 中点击查看；再次显示全部后所有配方完整恢复，无需重进存档。隐藏名单随世界存档保存并同步全体在线玩家，重进游戏仍生效。管理员还可用 `/icecore recipe exempt on|off|toggle` 进入或退出豁免模式，在该模式下 JEI 中恢复显示全部配方与物品、可正常查看和搜索（服务端合成禁止仍然生效）。同时安装 JEI 时自动启用 JEI 侧隐藏。
- 新增岛屿系统：管理员可把一片区域捕获为命名“岛”，随时真实隐藏（移除方块）与真实显现（放回方块）。可为岛录制一处入场镜头，显现时播放入场过场——黑屏过渡、切到定点镜头、屏幕螺旋扭曲逐渐增强，最后闪白一次性显现整座岛，带传送到达的观感；未录制镜头则直接显现。大型岛屿的生成过程分摊到多帧完成、不会卡服，且全程藏在特效之后，玩家看到的始终是完整岛屿。区域内方块或生物改动后，可重新扫描以更新岛的快照。管理员命令为 `/icecore island capture|recapture|camera set|camera clear|hide|show|remove`，框选复用建造区域功能。
